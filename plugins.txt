# The core marketplaces + plugins are PRE-DECLARED in .claude/settings.json
# (extraKnownMarketplaces + enabledPlugins). On first launch Claude Code prompts
# you once to TRUST each marketplace — click yes and the /ce-* and superpowers
# commands are live. No manual /plugin marketplace add / /plugin install needed
# for the core stack.
#
# This file is kept for the OPTIONAL plugins below. Uncomment and run the
# /plugin lines inside Claude Code only for what your project actually uses.
#
# TOKEN NOTE: every installed plugin loads its slash-command + skill descriptions
# into context at session start. Fewer plugins = leaner ambient context.

# === Already auto-trusted (declared in .claude/settings.json) ===
# Marketplaces:
#   compound-engineering-plugin   (EveryInc/compound-engineering-plugin)
#   superpowers-marketplace       (obra/superpowers-marketplace)
#   claude-code-skills            (alirezarezvani/claude-skills)
# Plugins enabled by default:
#   compound-engineering@compound-engineering-plugin   # /ce-* commands
#   superpowers@superpowers-marketplace

# === BUSINESS / PRODUCT — install for product-led projects (B2B, SaaS) ===
# /plugin install pm-skills@claude-code-skills
# /plugin install business-growth-skills@claude-code-skills

# === MARKETING / CONTENT — install for public-facing sites only ===
# /plugin install marketing-skills@claude-code-skills
# /plugin install content-creator@claude-code-skills

# === SECURITY — install if NOT already covered by your `core` skill profile ===
# Note: the `core` skills.txt profile already includes security-auditor agent.
# Only install this if you want the additional plugin-level skill.
# /plugin install skill-security-auditor@claude-code-skills

# === Verify ===
/plugin list


# ---- gstack is NOT a plugin. Install it manually (Git Bash) ----
#   git clone --single-branch --depth 1 https://github.com/garrytan/gstack.git "$env:USERPROFILE\.claude\skills\gstack"
#   cd "$env:USERPROFILE\.claude\skills\gstack" && bash ./setup
#
# ---- Real CE command names (corrects the blog post) ----
# /ce:brainstorm  -> /ce-brainstorm
# /ce:plan        -> /ce-plan
# /ce:work        -> /ce-work
# /ce:review      -> /ce-code-review
# /ce:compound    -> /ce-compound
